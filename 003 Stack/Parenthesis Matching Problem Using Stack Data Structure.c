#include <stdio.h>

// ---------------- THEORETICAL PART ----------------
// Parenthesis Matching Problem:
// → We check if every opening bracket '(' has a closing bracket ')' in correct order.
// → We use a Stack (LIFO - Last In First Out) data structure.
// → Algorithm:
//    1. Scan the string from left to right.
//    2. If '(' is found → push it into stack.
//    3. If ')' is found → pop from stack (check if stack is not empty).
//    4. After scanning, if stack is empty → parentheses are balanced.
//       else → not balanced.
// --------------------------------------------------
 
int main() {
    return 0;
}
